// import sortBy from 'lodash/sortBy'
// const { computeSpecifications } = require('dgad-base/lib/modules/dgad-utils/src/utils')

// const getSearchTypes = (state) => Object.keys(state.searches) || []
// const getCurrentField = (state) => state.searches[state.currentSearchType].fields[state.currentFieldName]
// const getFieldsBySearchType = (state) => state.searches[state.currentSearchType].fields
// const getOrderedFields = (state) => sortBy(getFieldsBySearchType(state), 'order') || []
// const getDisplayedFields = (state) => {
//   const orderedFields = getOrderedFields(state).filter((field) => !undisplayedFields.includes(field.name))
//   const hasFilledRequiredFields = orderedFields.every((field) => !field.required || field.selectedValue || field.selectedValue === 0)
// }

export default {
  // getSearchTypes,
  // getCurrentField,

}
