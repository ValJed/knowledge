// import axios from 'axios'

// const logError = (error) => {
//   if (process.env.NODE_ENV === 'development') {
//     console.error(error.message)
//   }
// }

// const getFieldValuesIfNeeded = (searches, searchType, fieldName) =>
//   searches[searchType].fields[fieldName].values.length === 0

// const postRequestApi = async (url, data) => {
//   try {
//     const options = {
//       url,
//       data,
//       method: 'POST',
//       timeout: 5000
//     }
//     await axios(options)
//   } catch (error) {
//     logError(error)
//   }
// }

module.exports = {
  // logError,

}
